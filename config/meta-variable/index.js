const TAG_TITLE = require('./meta-tag-title');
const APP_NAME = require('./meta-app-name');
const META_DESCRIPTION = require('./meta-description');
const META_KEYWORDS = require('./meta-keywords');
const NETWORK_ERROR_TEXT = require('./meta-network-error-text');
const META_THEME_COLOR = require('./meta-theme-color');

module.exports = {
  TAG_TITLE,
  APP_NAME,
  META_DESCRIPTION,
  META_KEYWORDS,
  NETWORK_ERROR_TEXT,
  META_THEME_COLOR,
}
